<UserControl x:Class="GUI.AkiTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!--<Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TreeView Grid.Column="0" x:Name="treeView" HorizontalContentAlignment="Stretch" SelectedItemChanged="treeView_SelectedItemChanged"/>
            <local:AkiNodeView Grid.Column="1" x:Name="nodeView" />
        </Grid>-->

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TreeView Grid.Row="0" x:Name="treeView" HorizontalContentAlignment="Stretch" SelectedItemChanged="treeView_SelectedItemChanged"/>
            <local:AkiNodeView Grid.Row="1" x:Name="nodeView" />
        </Grid>
    </Grid>
</UserControl>
